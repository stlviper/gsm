<div class="row">
  <div class="">
    <div id="manageRegisterMsg" class="alert {{#if isAlert}} alert-danger {{else}} alert-info {{/if}}
      {{#unless message}} hide {{/unless}}">
      {{message}}
    </div>
  </div>
</div>